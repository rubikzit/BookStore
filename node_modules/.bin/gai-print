#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../gai/gai-print" "$@"
  ret=$?
else 
  bash  "$basedir/../gai/gai-print" "$@"
  ret=$?
fi
exit $ret
