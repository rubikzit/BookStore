#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gai/gai-print.js" "$@"
  ret=$?
else 
  node  "$basedir/../gai/gai-print.js" "$@"
  ret=$?
fi
exit $ret
